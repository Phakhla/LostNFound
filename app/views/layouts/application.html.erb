<!DOCTYPE html>
<html>
  <head>
    <title>LostItem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'logo_nav.png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :meta %>
    <script src="https://kit.fontawesome.com/70f81e00e8.js" crossorigin="anonymous"></script>  
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=#{Figaro.env.MAPS_API_KEY}&libraries=places&callback=dispatchMapsEvent",
                                defer: true,
                                "data-turbolinks-eval": false %>

  </head>

  <body>
    <header>
      <%= render 'layouts/navbar' %>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
      <%= render 'layouts/footer' %>
    </footer>
  </body>
</html>
