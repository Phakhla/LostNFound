
<h1>My Profile</h1>
<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <% if current_user.avatar.present? %>
  <%= image_tag(current_user.avatar, style:"width: 150px; height: 150px; object-fit: cover;", class: "rounded-circle") %>
  <% else %>
  <img alt="image" class="rounded-circle" src="https://media.istockphoto.com/vectors/default-profile-picture-avatar-photo-placeholder-vector-illustration-vector-id1223671392?k=20&m=1223671392&s=612x612&w=0&h=lGpj2vWAI3WUT1JeJWm1PRoHT3V15_1pdcTn2szdwQ0=" style = "width: 150px; height: 150px; object-fit: cover;">
  <% end %>

  <div class="form-group">
    <label>Avatar</label>
    <div class="col-sm-8"><%= f.file_field :avatar, class: "form-control" %></div>
  </div>

  <div class="field">
    <label>อีเมล</label><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" , disabled: true %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <label>ชื่อ</label><br />
    <%= f.text_field :firstname, autofocus: true %>
  </div>
  <div class="field">
    <label>นามสกุล</label><br />
    <%= f.text_field :lastname, autofocus: true %>
  </div>

  <div class="field">
    <label>เบอร์โทรศัพท์</label><br />
    <%= f.text_field :tel, autofocus: true %>
  </div>

  <div class="field">
    <label>Facebook Link</label><br />
    <%= f.text_field :fblink, autofocus: true %>
  </div>

  <div class="field">
    <label>ที่อยู่</label><br />
    <%= f.text_field :address, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %> <br />
    <%= f.password_field :password, placeholder: "******", autocomplete: "new-password" , disabled: true %>

  </div>
<br>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
<%= link_to "Back", :back %>


<br><br>
<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>


